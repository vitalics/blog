import ExcalidrawWrapper from "./excalidraw-wrapper";

export const metadata = {
  title: "Excalidraw | Utils",
  description:
    "A virtual whiteboard for sketching diagrams and drawings. Everything runs in your browser — nothing is uploaded.",
};

export default function ExcalidrawPage() {
  return <ExcalidrawWrapper />;
}
